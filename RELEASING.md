## 🚀 נוהל שחרור גרסה

1. ודאו שהקבצים `VERSION` ו-`CHANGELOG.md` מעודכנים (SemVer + תאריך).
2. הריצו בדיקה ידנית: פתיחת `index.html`, סריקת מאגר, מעבר בין מצבי תאורה.
3. Commit בשם `chore(release): vX.Y.Z` ו-Tag תואם (`git tag vX.Y.Z`).
4. ודאו ש-GitHub Pages התעדכן (Action `Deploy static content to Pages` ירוץ אוטומטית).
5. פתחו PR עם תקציר: פיצ'רים, תיקונים, בדיקות, קבצי מדיה שנוספו.
